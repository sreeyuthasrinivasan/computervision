def put_text(text):
    img = np.ones((400,600,3),dtype=np.uint8)*255
    cv2.putText(img,text,(50,200),
                cv2.FONT_HERSHEY_SIMPLEX,
                1,(0,0,0),2)
    cv2.imshow("Text",img)
    cv2.waitKey(0)

put_text("Hello OpenCV")
